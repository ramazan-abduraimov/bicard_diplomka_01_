
import 'package:flutter/material.dart';

class InfoSurgery extends StatelessWidget {
  const InfoSurgery({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}