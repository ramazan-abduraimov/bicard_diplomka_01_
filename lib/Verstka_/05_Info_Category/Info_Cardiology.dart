
import 'package:flutter/material.dart';

class InfoCardiology extends StatelessWidget {
  const InfoCardiology({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}