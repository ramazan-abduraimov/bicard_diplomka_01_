
import 'package:flutter/material.dart';

class InfoHospitolization extends StatelessWidget {
  const InfoHospitolization({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}